@using SAEON.Observations.QuerySite.Models
@model SpacialCoverageModel
@(Html.EJ()
    .ListView("listViewSelectedFeatures")
    //.AutoAdjustHeight(true)
    //.AutoAdjustScrollHeight(true)
    .ShowHeader(true)
    .HeaderTitle("Selected")
    .DataSource(Model.SelectedFeatures)
    .FieldSettings(fs => fs.Text("Name")))

