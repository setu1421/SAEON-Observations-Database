@using SAEON.Observations.QuerySite.Models
@model QueryModel
@Html.EJ()
    .ListView("listViewSelectedLocations")
    .ShowHeader(true)
    .HeaderTitle("Selected")
    .DataSource(Model.SelectedLocations)
    .FieldSettings(fs => fs.Text("Name"))
